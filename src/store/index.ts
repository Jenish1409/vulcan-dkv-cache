/**
 * Public barrel export for the Vulcan store module.
 * Import from here in later phases (e.g. network layer, replication).
 */
export { LRUCache } from "./LRUCache";
export type { CacheOptions, GetResult } from "./LRUCache";
